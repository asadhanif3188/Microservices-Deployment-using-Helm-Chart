apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-commerce_microservices_helm_chart.fullname" . }}
  labels:
    {{- include "e-commerce_microservices_helm_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e-commerce_microservices_helm_chart.selectorLabels" . | nindent 4 }}
