apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e-commerce_microservices_helm_chart.fullname" . }}-test-connection"
  labels:
    {{- include "e-commerce_microservices_helm_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "e-commerce_microservices_helm_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
